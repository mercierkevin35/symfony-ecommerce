{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - {{ SHOP_NAME }}{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    <h2>Bienvenue {{ app.user.firstname }} {{ app.user.lastname}}</h2>
    <p>
        Modifie ton mot de passe maintenant batard
    </p>
    <hr>
      <a href="{{ path('account') }}">Retourner à mon compte</a>
    <hr>
    {% if notification %}
    <div class="alert alert-info">
        {{ notification }}
    </div>
    {% endif %}
    {{ form(form) }}
{% endblock %}
